class UserDataModel{
  int id;
  int users_id;
  String email;
  String full_name;
  int phone;
  String role;

  UserDataModel(this.id, this.users_id, this.email, this.full_name, this.phone, this.role);
}